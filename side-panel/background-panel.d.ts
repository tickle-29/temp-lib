/// <reference types="react" />
import { StoreType } from '../model/store';
export declare const BackgroundPanel: (({ store }: {
    store: StoreType;
}) => JSX.Element) & {
    displayName: string;
};
